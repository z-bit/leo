export interface DataLaufend {
    nr: string;     // Inventur Nummer
    bez: string;    // Inventur Bezeichnung
    z1z: number;    // Zählung 1 gezählt
    z1p: number;    // Zählung 1 Positionen
    z2z: number;    // Zählung 2 gezählt
    z2p: number;    // Zählung 2 Positionen
}